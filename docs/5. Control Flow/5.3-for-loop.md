# 5.3 For Loop

Pilang `for` loops iterate over iterable values.

```pilang
for item in [1, 2, 3] {
    println(item);
}
```

The loop variable is scoped to the loop body.

## Syntax

Parentheses are optional.

```pilang
let values = [1, 2, 3];

for item in values {
    println(item);
}

for (item in values) {
    println(item);
}
```

The left side must be an identifier.

```pilang
for i in 0..5 {
    println(i);
}
```

## Single-Statement Bodies

If the body is one statement, braces are optional.

```pilang
for i in 0..5
    println(i);
```

## Ranges

Ranges are a common iterable for numeric loops.

```pilang
for i in 0..5 {
    println(i); // 0, 1, 2, 3, 4
}
```

The range end is exclusive during iteration. Add a step with `:`.

```pilang
for i in 0..10:2 {
    println(i); // 0, 2, 4, 6, 8
}

for i in 5..0:-1 {
    println(i); // 5, 4, 3, 2, 1
}
```

## Lists, Tuples, Strings, Sets

Lists and tuples yield their items.

```pilang
for value in [10, 20, 30] {
    println(value);
}

for value in ("a", "b", "c") {
    println(value);
}
```

Strings yield one-character strings.

```pilang
for ch in "abc" {
    println(ch);
}
```

Sets are iterable, but their order is not guaranteed.

```pilang
for value in {"red", "green", "blue"} {
    println(value);
}
```

## Maps

Maps iterate over keys.

```pilang
let user = {"name": "Ada", "active": true};

for key in user {
    println(key + ": " + user[key]);
}
```

## Nested For Loops

Loops can be nested.

```pilang
for row in 0..3 {
    for col in 0..3 {
        println(row + ", " + col);
    }
}
```

`break` and `continue` apply to the nearest enclosing loop.
