Point = {
    constructor(x, y) {
        this.x = x;
        this.y = y;
    },
    tostring() {
        return "(" + this.x + ", " + this.y + ")";
    },
    valueof() {
        return this.x + this.y;
    }
}

point = Point(1, 2)

println(point + 3)
println("point=" + point)
println(+point)
