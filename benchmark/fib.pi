// Recursive calls and branch-heavy integer work.
fun main() {
    let fib = fun(n) {
        if n <= 1 {
            return n;
        }
        return fib(n - 1) + fib(n - 2);
    }

    let t0 = time()
    let result = fib(28);
    println(time() - t0)
}

main()
