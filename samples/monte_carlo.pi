fun monte_carlo(nsamples)
{
    let acc = 0
    let i = nsamples
    while i > 0
    {
        let x = rand()
        let y = rand()
        if (x ** 2 + y ** 2) < 1.0        
            acc += 1
        i--
    }
    return 4.0 * acc / nsamples
}

println(monte_carlo(10000000))