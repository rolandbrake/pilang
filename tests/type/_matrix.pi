import matrix.{zeros, ones, eye, mult, size, is_mat};

let z = zeros(3, 3)
let o = ones(3, 3)
let e = eye(3, 3)

println(z)
println(o)
println(e)
println(size(e))
println(is_mat(e))
println(mult(eye(2, 2), ones(2, 2)))
println(eye(2, 2) * ones(2, 2))
println(e * e)
