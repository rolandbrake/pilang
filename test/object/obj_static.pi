Point = {
    c: 10, 
    _private: 3,
    constructor(x, y) {
        this.x = x;
        this.y = y;
    }
}

point = Point(1, 2);
point.c += 1
other = Point(3, 4);

println(point.c)
println(other.c)
println(Point.c)
