class Point {
    constructor(x, y) {
        this.x = x
        this.y = y
    }

    format() {
        return "(" + this.x + ", " + this.y + ")"
    }
}

point = Point(1, 2)
println(point.format())
println("point=" + point.format())
