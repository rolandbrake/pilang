class Point {
    X = 1
    Y = 2
}

assert(Point.name() == "Point", "classes should expose their class name")
Point.setName("RenamedPoint")
assert(Point.name() == "RenamedPoint", "class names should be mutable")
println(Point.name())
